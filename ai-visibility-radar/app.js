const form=document.getElementById('audit-form');const statusEl=document.getElementById('form-status');form?.addEventListener('submit',async(e)=>{e.preventDefault();const data=new FormData(form);const request=[`AI Visibility Radar — Ücretsiz Mini Analiz Talebi`,`Marka: ${data.get('brand')}`,`Web sitesi: ${data.get('website')}`,`Ülke: ${data.get('country')}`,`Sektör: ${data.get('industry')}`,`Rakip: ${data.get('competitor')||'Belirtilmedi'}`,`E-posta: ${data.get('email')}`].join('\n');try{await navigator.clipboard.writeText(request);statusEl.textContent='Analiz talebiniz kopyalandı. Canlı başvuru sistemi bir sonraki aşamada bağlanacak.'}catch{statusEl.textContent='Analiz talebiniz hazırlandı. Canlı başvuru sistemi bir sonraki aşamada bağlanacak.'}});