const form=document.getElementById('audit-form');const statusEl=document.getElementById('form-status');form?.addEventListener('submit',async(e)=>{e.preventDefault();const data=new FormData(form);const request=[`AI Visibility Radar — Free Mini Audit Request`,`Brand: ${data.get('brand')}`,`Website: ${data.get('website')}`,`Country: ${data.get('country')}`,`Industry: ${data.get('industry')}`,`Competitor: ${data.get('competitor')||'Not provided'}`,`Email: ${data.get('email')}`].join('\n');try{await navigator.clipboard.writeText(request);statusEl.textContent='Request copied. Live lead intake will be connected next.'}catch{statusEl.textContent='Request prepared. Live lead intake will be connected next.'}});